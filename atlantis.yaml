projects:
- dir: .
  workflows:
    custom:
      init:
        steps:
        -run: terraform init -input=false -upgrade
      plan:
        steps:
        - init
        -run: terraform plan -input=false
    